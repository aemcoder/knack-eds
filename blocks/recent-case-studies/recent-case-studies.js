/*
 * Alias block: case-study pages author "recent-case-studies"; the
 * implementation lives in recent-posts (which defaults to the case-study
 * query index on /case-study/* pages and tags the block for shared CSS).
 */
import decorate from '../recent-posts/recent-posts.js';

export default decorate;
